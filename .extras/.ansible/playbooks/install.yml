---
- name: Install the capture_stdout_and_stderr function support script
  hosts: localhost
  tasks:
    - name: Install the capture_stdout_and_stderr function support script
      include_role:
        name: adrianjuhl.shell_capture_stdout_and_stderr
        apply:
          become: true
